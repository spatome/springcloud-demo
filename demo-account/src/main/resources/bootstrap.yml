eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 15
  client:
    service-url:
      #defaultZone: http://root:root@192.168.10.133:9999/eureka/
      defaultZone: http://root:root@127.0.0.1:9999/eureka/
    registry-fetch-interval-seconds: 5

spring:
  cloud:
    config:
      name: config  #配置文件config-dev.properties
      profile: dev
      username: root
      password: root
#      uri:
#        - http://192.168.0.199:39998
      discovery:
        enabled: true
        service-id: DEMO-CLOUD-CONFIG
      label: master

#spring:
#  application:
#    name: demo-test
#  cloud:
#    config:
#      #profile: dev
#      name: config-dev
#      discovery:
#        enabled: true
#        service-id: DEMO-CONFIG
#    bus:
#      enabled: true
#      trace:
#        enabled: true
#
#management:
#  endpoints:
#    web:
#      exposure:
#        include: bus-refresh

feign:
  hystrix:
    enabled: true
  client:
    config:
      default:
        connectTimeout: 3000
        readTimeout: 30000

hystrix:
  command:
    default:
      execution:
        isolation: 
          thread:
            timeoutInMilliseconds: 60000